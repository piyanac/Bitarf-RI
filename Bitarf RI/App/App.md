# App reconstruction prompts

## `DocumentBrowserView.swift` (insertion markers retained)

Keep the generic browser, library, settings shell, launch behavior, and document-default persistence. At the marked default-settings locations restore classic-model defaults: density level 3, trailing feed 300 dots, density range one through five, trailing-feed range 0 through 2,000 dots, and fixed-axis values constrained by the reconstructed hardware profile.

